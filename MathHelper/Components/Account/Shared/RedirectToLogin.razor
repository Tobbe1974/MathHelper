@inject NavigationManager NavigationManager
@inject PathAwareNavigationManager PathNav
@using MathHelper.Services

@code {
    protected override void OnInitialized()
    {
        PathNav.NavigateTo($"Account/Login?returnUrl={Uri.EscapeDataString(NavigationManager.Uri)}", forceLoad: true);
    }
}
